# assets/ — Static Assets

Static CSS and asset files used by the legacy HTML frontend.

## Structure

```
assets/
└── css/    # Stylesheets
```

## Notes

- Used by `index.html` at the project root (legacy HTML frontend)
- The Vue.js frontend has its own styles in `frontend/src/style.css`
